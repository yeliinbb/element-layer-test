import { DrawSection, SideBar } from '..';
import { StMain } from '../../styles';

const DrawAlignView = () => {
  return (
    <StMain>
      <SideBar />
      <DrawSection />
    </StMain>
  );
};

export default DrawAlignView;
