import { Canvas, SideBar } from '..';
import { StMain } from '../../styles';

const CanvasEditorLayout = () => {
  return (
    <StMain>
      <SideBar />
      <Canvas />
    </StMain>
  );
};

export default CanvasEditorLayout;
